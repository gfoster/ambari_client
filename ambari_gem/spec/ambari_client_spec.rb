require 'spec_helper'

describe AmbariClient do
  it 'has a version number' do
    expect(AmbariClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
